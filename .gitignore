__pycache__/
*.pyc
*.swp
*.swo
.DS_Store
logs/
models/

# Training datasets live next to their toml as tasks/<name>/, but the
# materials themselves are not tracked. The toml files (tasks/*.toml) and
# the .gitkeep stay tracked.
tasks/*/
